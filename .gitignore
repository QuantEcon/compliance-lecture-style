# Jupyter Book build output
lectures/_build/
# Python
__pycache__/
*.pyc
.venv/
# Corpus clones: blobless sparse checkouts, kept inside the tree so an
# unattended run needs no permission prompt to read them
# (see .claude/skills/pass-measure/SKILL.md § 2)
.corpus/
